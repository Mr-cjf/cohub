# =============================================================================
# 历史遗留文件：packages/oh-my-opencode-cohub/.github/workflows/publish.yml
# =============================================================================
#
# 本文件位于子包内，GitHub Actions 只加载仓库根 .github/workflows/ 目录，
# 因此本文件自创建以来从未被 GitHub Actions 加载执行，当前不生效。
#
# 自 monorepo 改造后，oh-my-opencode-cohub 的发布工作流已迁移至仓库根：
#   .github/workflows/publish.yml（唯一生效的发布流水线）
#
# 本包对应的 tag 前缀：opencode-v*
# 手动触发方式：推送 opencode-v* 格式的 tag（如 opencode-v1.0.0），
# 或在 GitHub Actions 页面用 workflow_dispatch 选择本包（oh-my-opencode-cohub），
# 根级工作流已为三个包（oh-my-opencode-cohub / dsh-cohub / cohub）都支持。
#
# 保留此文件是为了记录历史配置，便于回溯：原工作流定义包括
# checkout@v4 / setup-bun@v2 / setup-node@v4 / npm install / bun run build /
# tag 版本校验 / npm publish --provenance --access public（自动检测 prerelease tag）。